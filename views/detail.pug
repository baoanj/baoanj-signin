extends detail_layout

block content
  #main_content
    h1 用户详情
    #textContent
      div
        p 用户名 ：
        p#username!= user.username
      hr
      div
        p 邮箱 ：
        p#email!= user.email
    p#switch
      a(href='/user/signout', title='退出登录') 退出登录
